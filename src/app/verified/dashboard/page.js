import React from 'react'

export const metadata = {
  title: "Dashboard",
  description: "Access the Growmax admin dashboard to manage users, blog content, settings, and platform operations from one centralized interface."
}

const page = () => {
  return (
    <div></div>
  )
}

export default page